<Window x:Class="KDSUI.Windows.PasswordWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Password Needed" Height="150" Width="300"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Label and TextBox for the station name -->
        <TextBlock Text="Enter Password:" Grid.Row="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,5,0,5" Height="25"/>
        <!-- Buttons at the bottom -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="Done" Width="70" Margin="0,0,5,0" Click="Done_Click"/>
        </StackPanel>
    </Grid>
</Window>
